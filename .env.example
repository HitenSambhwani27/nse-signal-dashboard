# Browser fetches same-origin `/api/v1/*` (Next.js route proxy).
# Configure the FastAPI upstream here (SSH tunnel to the VM).
NEXT_PUBLIC_API_BASE_URL=
API_BASE_URL=http://127.0.0.1:8080
NSE_API_URL=http://127.0.0.1:8080
