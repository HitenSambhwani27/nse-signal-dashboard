"""The client terminal is the Next.js app. Run `npm run dev` from the repo root."""

from __future__ import annotations


def main() -> None:
    raise SystemExit(
        "Streamlit UI retired. From the repository root: npm install && npm run dev"
    )


if __name__ == "__main__":
    main()
