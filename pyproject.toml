[build-system]
requires = ["setuptools>=68.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "nse-signal-dashboard"
version = "0.2.0"
description = "Read-only NSE market terminal. Talks only to the pipeline UI API."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.27.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0.0",
]

[tool.setuptools.packages.find]
where = ["src"]
include = ["nse_dashboard*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
